{% block content %}
    <body>
    <div>
        Dobrý den,
        <br><br>
        {{ task.owner }} přidal nový úkol "{{ task }}" k Vaší
        <a href="{{ absolute_url(path('work_detail', {'_locale': default_locale, 'id': work.id | hashids_encode })) }}"> práci</a>.
        <br><br>
        S pozdravem
        <br><br>
        Systém pro vedení bakalářských a diplomových prací
    </div>
    </body>
{% endblock %}
