{% block content %}
    <body>
    <div>
        Dobrý den,
        <br><br>
        {{  mediaOwner }} zaktualizoval údaje verzi "{{ mediaName }}" u
        <a href="{{ absolute_url(path('work_detail', {'_locale': default_locale, 'id': workId | hashids_encode })) }}">
            práci "{{ workTitle }}".
        </a>
        <br><br>
        S pozdravem
        <br><br>
        Systém pro vedení bakalářských a diplomových prací
    </div>
    </body>
{% endblock %}
